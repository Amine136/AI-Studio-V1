rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // NovaNode no longer stores privileged application data in Firestore.
    // Keep Firestore locked down unless a future server-managed use case is added.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
